export const database = async () => {
    try {
        // eslint-disable-next-line no-console
        console.log('Database need to configure')
    } catch (error) {
        throw new Error(error)
    }
}
